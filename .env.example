# Copy this file to .env and fill in your own values.
# Never commit .env or paste real keys into chat/code you share.

PORT=3000

# Optional: only needed if you wire src/ai-providers.js to a real
# AI video/image/voice generation provider. Leave empty to use the
# built-in placeholder clips for "AI" timeline steps.
AI_VIDEO_API_KEY=
